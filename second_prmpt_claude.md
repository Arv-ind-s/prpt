Run the scaffold commands from your step 4. Do not implement any task yet,
and do not create stub files just to make imports resolve.

Then run the harness and paste the actual terminal output verbatim — not a
summary of it.

Confirm each test fails because the implementation is missing, not because of
a tooling, import, config, or dependency error. Call out explicitly any test
that fails for the wrong reason, and any that error before the test body runs.

Do not modify anything under harness/. Then stop.
