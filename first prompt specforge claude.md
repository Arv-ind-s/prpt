Read these in full before doing anything: CLAUDE.md, SPEC.md, PLAN.md,
TASKS.md, and every file under harness/ (not just HARNESS.md).

Do not write implementation code yet. Report back, briefly:
1. The pinned stack and the frozen interface contracts, restated in your words.
2. The ordered task list with each task's acceptance command.
3. Any contradiction or gap between the four documents — especially any test
   file in harness/ that references a module, path, or symbol that no task
   tells you to create.
4. The exact scaffold commands to get the harness executing and failing for
   the right reason (missing implementation, not missing tooling).

Then stop and wait for my go-ahead. Do not scaffold yet
